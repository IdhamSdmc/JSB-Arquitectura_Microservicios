spring:
  application:
    name: gateway-service
  cloud:
    config:
      name: gateway-service
      uri: http://config-service:8081
      username: root
      password: s3cr3t
      retry:
        max-attempts: 10
        initial-interval: 5000
      fail-fast: true



